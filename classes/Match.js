export default class Match {
  constructor(home, visitor, config) {
    this.home = home;
    this.visitor = visitor;
    this.setup(config);
  }

  setup(config){
  }

  play_match(){
  }

  set_match_final_score(home_points, visitor_points){

  }
}